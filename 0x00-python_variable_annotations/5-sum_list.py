#!/usr/bin/env python3
""" returns the floor of the float. """


def sum_list(input_list: list) -> float:
    """ returns the floor of the float. """
    sum_total = 0
    for num in input_list:
        sum_total += num
    return sum_total
